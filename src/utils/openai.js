export async function getAIResponse(step, userInput, template) {
  // ここは実際のAPI呼び出しに置き換えてください
  // 例: fetchでOpenAI APIにPOST
  // ここではダミー応答
  return `「${userInput}」ですね。とても良い視点です！もしよければ、${template} も参考にしてみてください。`;
}
